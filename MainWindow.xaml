<hc:Window x:Class="MeasurementSoftware.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MeasurementSoftware"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        mc:Ignorable="d"
        Title="SF-GAMS通用自动测量系统" Height="600" Width="1000"
        xmlns:ctls="clr-namespace:MeasurementSoftware.Extensions"
        xmlns:core="clr-namespace:MeasurementSoftware.Models"
        WindowStartupLocation="CenterScreen">
    <hc:Window.Resources>

        <!-- 主菜单按钮样式 -->
        <Style x:Key="MainMenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="12,12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" 
                            Background="{TemplateBinding Background}" 
                            CornerRadius="4" 
                            Margin="4,2">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                            VerticalAlignment="Center" 
                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#F0F0F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 子菜单按钮样式 -->
        <Style x:Key="SubMenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="32,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" 
     Background="{TemplateBinding Background}" 
           CornerRadius="4" 
       Margin="4,1">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                 VerticalAlignment="Center" 
    Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#F0F0F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 带动画的 Expander 样式 -->
        <Style x:Key="AnimatedExpanderStyle" TargetType="Expander" BasedOn="{StaticResource {x:Type Expander}}">
            <Style.Triggers>
                <Trigger Property="IsExpanded" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <!-- 缩放动画 - 缓慢叠上去 -->
                                <DoubleAnimation 
      Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)" From="0.1" To="1" 
        Duration="0:0:0.35">
                                    <DoubleAnimation.EasingFunction>
                                        <QuadraticEase EasingMode="EaseOut"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                                <!-- Y轴位移动画 - 向上滑入 -->
                                <DoubleAnimation 
                    Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[1].(TranslateTransform.Y)" From="-10" To="0" Duration="0:0:0.35">
                                    <DoubleAnimation.EasingFunction>
                                        <QuadraticEase EasingMode="EaseOut"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- GridSplitter 样式 -->
        <Style x:Key="ResizeGridSplitterStyle" TargetType="GridSplitter">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GridSplitter">
                        <Border Background="{TemplateBinding Background}"
                                Cursor="SizeWE">
                            <Rectangle Width="1" 
                                     Fill="{DynamicResource BorderBrush}" 
                                     Opacity="0.5"
                                     HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CaptionIconButtonStyle" TargetType="hc:ContextMenuButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="43"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="hc:ContextMenuButton">
                        <Border x:Name="Border" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#E2E2E2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </hc:Window.Resources>

    <hc:Window.NonClientAreaContent>
        <Grid Height="29">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <hc:ContextMenuButton Grid.Column="1"
                                  
                                  Style="{StaticResource CaptionIconButtonStyle}"
                                  ToolTip="设置">
                <hc:ContextMenuButton.Content>
                    <TextBlock Text="&#xE713;" 
             FontFamily="Segoe MDL2 Assets" 
             FontSize="16"
             Foreground="Black"
                              
             HorizontalAlignment="Center"
             VerticalAlignment="Center"/>
                </hc:ContextMenuButton.Content>
                <hc:ContextMenuButton.Menu>
                    <ContextMenu>
                        <MenuItem Header="通道配置" Command="{Binding NavigateToChannelSettingCommand}"/>
                        <MenuItem Header="设备管理" Command="{Binding NavigateToCommunicationSettingCommand}"/>
                        <MenuItem Header="二维码配置" Command="{Binding NavigateToQrCodeSettingCommand}"/>
                        <MenuItem Header="MES配置" Command="{Binding NavigateToMesSettingCommand}"/>
                        <Separator/>
                        <MenuItem Header="其他设置" Command="{Binding NavigateToOtherSettingsCommand}"/>
                    </ContextMenu>
                </hc:ContextMenuButton.Menu>
            </hc:ContextMenuButton>
        </Grid>
    </hc:Window.NonClientAreaContent>

    <Grid Background="{DynamicResource SecondaryRegionBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="LeftColumn" Width="250" MinWidth="200" MaxWidth="300"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid x:Name="GridLeft" Grid.Column="0" Margin="16,16,0,16">
            <Grid.RenderTransform>
                <TranslateTransform x:Name="LeftTransform" X="0"/>
            </Grid.RenderTransform>
            <Border Background="{DynamicResource RegionBrush}" CornerRadius="10">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="0" BlurRadius="10" Color="{DynamicResource ShadowColor}" Opacity=".1"/>
                </Border.Effect>
            </Border>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="80"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center"  HorizontalAlignment="Left" Margin="30">
                    <TextBlock VerticalAlignment="Center" Margin="0,0,12,0">
                <InlineUIContainer>
                    <Image Source="/Images/ico.ico"  Width="24" Height="24"/>
                </InlineUIContainer>
                    </TextBlock>
                    <TextBlock Text="{Binding Title}" 
                               FontSize="18"   
                               Foreground="{DynamicResource PrimaryTextBrush}" 
                               FontWeight="SemiBold" 
                               VerticalAlignment="Center"/>
                </StackPanel>

                <ScrollViewer Grid.Row="1" Margin="8,0,8,16" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                    <StackPanel>
                        <Button x:Name="HomeButton"  Command="{Binding NavigateToHomeCommand}" Click="MenuButton_Click"
                                Style="{StaticResource MainMenuButtonStyle}">
                            <StackPanel Orientation="Horizontal"  Margin="10 0 0 0">
                                <TextBlock Text="&#xE80F;" FontFamily="Segoe MDL2 Assets" FontSize="16" Margin="0,0,12,0"/>
                                <TextBlock Text="测量" FontSize="14"/>
                            </StackPanel>
                        </Button>



                        <Button x:Name="CalibrationButton" Command="{Binding NavigateToCalibrationCommand}" Click="MenuButton_Click"
                                Style="{StaticResource MainMenuButtonStyle}">
                            <StackPanel Orientation="Horizontal"  Margin="10 0 0 0">
                                <TextBlock Text="&#xE9E9;" FontFamily="Segoe MDL2 Assets" FontSize="16" Margin="0,0,12,0"/>
                                <TextBlock Text="校准" FontSize="14"/>
                            </StackPanel>
                        </Button>

                        <Button x:Name="DataManagementButton" Command="{Binding NavigateToDataManagementCommand}" Click="MenuButton_Click"
                                Style="{StaticResource MainMenuButtonStyle}">
                            <StackPanel Orientation="Horizontal"  Margin="10 0 0 0">
                                <TextBlock Text="&#xE8F1;" FontFamily="Segoe MDL2 Assets" FontSize="16" Margin="0,0,12,0"/>
                                <TextBlock Text="数据管理" FontSize="14"/>
                            </StackPanel>
                        </Button>

                        <Button x:Name="SpcButton" Command="{Binding NavigateToSpcCommand}" Click="MenuButton_Click"
                                Style="{StaticResource MainMenuButtonStyle}">
                            <StackPanel Orientation="Horizontal"  Margin="10 0 0 0">
                                <TextBlock Text="&#xE9D9;" FontFamily="Segoe MDL2 Assets" FontSize="16" Margin="0,0,12,0"/>
                                <TextBlock Text="SPC分析" FontSize="14"/>
                            </StackPanel>
                        </Button>

                        <Expander x:Name="SettingsExpander"
                            Header="设置" 
                            Expanded="Expander_Expanded" 
                            Background="Transparent"
                            BorderBrush="Transparent"
                             Style="{StaticResource AnimatedExpanderStyle}">
                            <Expander.RenderTransform>
                                <TransformGroup>
                                    <ScaleTransform ScaleY="1"/>
                                    <TranslateTransform Y="0"/>
                                </TransformGroup>
                            </Expander.RenderTransform>
                            <Expander.HeaderTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="16,12">
                                        <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="16" Margin="0,0,12,0"/>
                                        <TextBlock Text="{Binding}" FontSize="14"/>
                                    </StackPanel>
                                </DataTemplate>
                            </Expander.HeaderTemplate>
                            <StackPanel>
                                <Button x:Name="ChannelSettingButton" 
                                 Click="MenuButton_Click"  Command="{Binding NavigateToChannelSettingCommand}"
                                 Style="{StaticResource SubMenuButtonStyle}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="&#xE762;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,12,0"/>
                                        <TextBlock Text="通道配置" FontSize="13"/>
                                    </StackPanel>
                                </Button>
                                <Button x:Name="CommunicationButton" 
                                 Click="MenuButton_Click"  Command="{Binding NavigateToCommunicationSettingCommand}"
                                 Style="{StaticResource SubMenuButtonStyle}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="&#xE968;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,12,0"/>
                                        <TextBlock Text="设备管理" FontSize="13"/>
                                    </StackPanel>
                                </Button>
                                <Button x:Name="QrCodeSettingButton" 
                                 Click="MenuButton_Click"  Command="{Binding NavigateToQrCodeSettingCommand}"
                                 Style="{StaticResource SubMenuButtonStyle}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="&#xEAD8;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,12,0"/>
                                        <TextBlock Text="二维码配置" FontSize="13"/>
                                    </StackPanel>
                                </Button>
                                <Button x:Name="MesSettingButton" 
                                 Click="MenuButton_Click"  Command="{Binding NavigateToMesSettingCommand}"
                                 Style="{StaticResource SubMenuButtonStyle}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="&#xE774;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,12,0"/>
                                        <TextBlock Text="MES配置" FontSize="13"/>
                                    </StackPanel>
                                </Button>
                                <Button x:Name="OtherSettingsButton" 
                                 Click="MenuButton_Click"  Command="{Binding NavigateToOtherSettingsCommand}"
                                 Style="{StaticResource SubMenuButtonStyle}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,12,0"/>
                                        <TextBlock Text="其他设置" FontSize="13"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </Expander>



                        <Button x:Name="RecipeManagementButton" Command="{Binding NavigateToRecipeManagementCommand}" Click="MenuButton_Click"
                            Style="{StaticResource MainMenuButtonStyle}">
                            <StackPanel Orientation="Horizontal"  Margin="10 0 0 0">
                                <TextBlock Text="&#xE7C3;" FontFamily="Segoe MDL2 Assets" FontSize="16" Margin="0,0,12,0"/>
                                <TextBlock Text="配方管理" FontSize="14"/>
                            </StackPanel>
                        </Button>
                        <Button x:Name="LogViewerButton" Click="MenuButton_Click" Command="{Binding NavigateToLogViewerCommand}"
                             Style="{StaticResource MainMenuButtonStyle}">
                            <StackPanel Orientation="Horizontal" Margin="10 0 0 0">
                                <TextBlock Text="&#xE7BA;" FontFamily="Segoe MDL2 Assets" FontSize="16" Margin="0,0,12,0"/>
                                <TextBlock Text="日志" FontSize="14"/>
                            </StackPanel>
                        </Button>


                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Grid>

        <!-- GridSplitter 用于拖拽调整左侧菜单宽度 -->
        <GridSplitter Grid.Column="1"
              Width="4"
     HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
        Style="{StaticResource ResizeGridSplitterStyle}"
      DragCompleted="GridSplitter_DragCompleted"/>

        <Button Grid.Column="1"
       x:Name="ButtonShiftOut" 
     Click="OnLeftMainContentShiftOut" 
                HorizontalAlignment="Left" 
          VerticalAlignment="Top" 
  Margin="0,30,0,0" 
        Width="15"
           Height="30"
       Background="{DynamicResource RegionBrush}"
BorderBrush="{DynamicResource BorderBrush}"
          BorderThickness="0,1,1,1" Grid.ColumnSpan="2">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
       BorderBrush="{TemplateBinding BorderBrush}"
      BorderThickness="{TemplateBinding BorderThickness}"
        CornerRadius="0,15,15,0">
                        <Path Data="{StaticResource LeftGeometry}" 
            Fill="{DynamicResource PrimaryTextBrush}"
              Width="8" Height="8"
      HorizontalAlignment="Center"
      Stretch="Uniform"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <Button Grid.ColumnSpan="3"
                x:Name="ButtonShiftIn" 
                Click="OnLeftMainContentShiftIn" 
                HorizontalAlignment="Left" 
                VerticalAlignment="Top" 
                Margin="0,30,0,0" 
                Width="15"
                Height="30"
                Visibility="Collapsed"
                Background="{DynamicResource RegionBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,1,1">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="0,15,15,0">
                        <Path Data="{StaticResource RightGeometry}" 
                              Fill="{DynamicResource PrimaryTextBrush}"
                              Width="8" Height="8"
                              HorizontalAlignment="Center"
                              Stretch="Uniform"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <Border 
            x:Name="MainContent" Grid.Column="2"
        Margin="20,16,16,16" Background="{DynamicResource RegionBrush}" CornerRadius="10"
      BorderBrush="{DynamicResource BorderBrush}"
      BorderThickness="1,0,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>

                </Grid.RowDefinitions>

                <hc:TabControl
              ItemsSource="{Binding Tabs}"
              SelectedItem="{Binding SelectedTab, Mode=TwoWay}"
              OverflowMenuDisplayMemberPath="Header"
              IsAnimationEnabled="True"
              IsDraggable="True"
              ShowCloseButton="True"
              TabItemWidth="120"
              FontSize="12"
              BorderThickness="1"
              HorizontalAlignment="Stretch"
              VerticalAlignment="Stretch"
              Background="{DynamicResource RegionBrush}"
              SelectionChanged="TabControl_SelectionChanged"
              PreviewMouseRightButtonDown="TabControl_PreviewMouseRightButtonDown">

                    <!-- 定义资源 -->
                    <hc:TabControl.Resources>
                        <!-- AntDesign 模板 -->
                        <DataTemplate x:Key="AntDesignIconTemplate">
                            <TextBlock 
                          FontFamily="{StaticResource SystemIconFont}" 
                          FontSize="14" 
                          Text="{Binding Icon}" 
                          Margin="0,0,5,0"
                          VerticalAlignment="Center"/>
                        </DataTemplate>

                        <!-- FontAwesome 模板 -->
                        <DataTemplate x:Key="FontawesomeIconTemplate">
                            <ctls:FontAwesomeIcon 
                          Icon="{Binding Icon}" 
                          FontSize="14"
                          Margin="0,0,5,0" 
                          VerticalAlignment="Center"/>
                        </DataTemplate>

                        <Style x:Key="CusTabItemPlusBaseStyle" TargetType="hc:TabItem" BasedOn="{StaticResource TabItemPlusBaseStyle}">
                            <Setter Property="BorderThickness" Value="0,0,1,1"/>
                            <Setter Property="Background" Value="{DynamicResource RegionBrush}"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="hc:TabItem">
                                        <Grid x:Name="templateRoot" SnapsToDevicePixels="true" ContextMenu="{TemplateBinding Menu}">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <!-- 外层边框 -->
                                            <Border Grid.ColumnSpan="3" BorderThickness="{TemplateBinding BorderThickness}" x:Name="mainBorder" BorderBrush="{TemplateBinding BorderBrush}" Background="{TemplateBinding Background}" Margin="0">
                                                <Border Margin="0,0,0,-1" x:Name="innerBorder" Background="{DynamicResource RegionBrush}" Visibility="Collapsed" />
                                            </Border>

                                            <!-- 图标 -->
                                            <Path x:Name="PathMain" Margin="10,0,0,0" Grid.Column="0" Width="{TemplateBinding hc:IconElement.Width}" Height="{TemplateBinding hc:IconElement.Height}" Fill="{TemplateBinding Foreground}" SnapsToDevicePixels="True" Stretch="Uniform" Data="{TemplateBinding hc:IconElement.Geometry}" />

                                            <!-- Header 内容 -->
                                            <ContentPresenter Grid.Column="1" x:Name="contentPresenter" ContentSource="Header" Focusable="False" HorizontalAlignment="Stretch" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="Center" />

                                            <!-- 遮罩 -->
                                            <Border Name="BorderMask" Grid.Column="1" HorizontalAlignment="Right" Width="20" Background="{TemplateBinding Background}" Margin="0,0,1,1">
                                                <Border.OpacityMask>
                                                    <LinearGradientBrush EndPoint="1,0" StartPoint="0,0">
                                                        <GradientStop Color="White" Offset="1" />
                                                        <GradientStop Offset="0" />
                                                    </LinearGradientBrush>
                                                </Border.OpacityMask>
                                            </Border>

                                            <!-- 关闭按钮 -->
                                            <Button 
                                          Grid.Column="2"
                                          Focusable="False"
                                          Command="{Binding CloseTabCommand}"
                                          CommandParameter="{Binding}"
                                          Background="Transparent"
                                          Width="28"
                                          Visibility="{Binding IsClosable, Converter={StaticResource Boolean2VisibilityConverter}}"
                                          OverridesDefaultStyle="True"
                                          Cursor="Hand">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="Button">
                                                        <!-- 使用稍大的容器保证圆形和图标居中 -->
                                                        <Grid Width="28" Height="28" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="1">
                                                            <!-- 圆形 hover 背景（默认透明） -->
                                                            <Ellipse x:Name="bg"
                                                          Width="16"
                                                          Height="16"
                                                          Fill="Transparent"
                                                          HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>
                                                            <Path x:Name="icon"
                                                          Style="{StaticResource ClosePathStyle}"
                                                          Width="8"
                                                          Height="8"
                                                          Fill="{DynamicResource PrimaryTextBrush}"
                                                          HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"
                                                          SnapsToDevicePixels="True"/>
                                                        </Grid>

                                                        <ControlTemplate.Triggers>
                                                            <!-- Hover：显示圆形背景并加深图标颜色 -->
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="bg" Property="Fill" Value="#E5E5E5"/>
                                                                <Setter TargetName="icon" Property="Fill" Value="#333"/>
                                                            </Trigger>

                                                            <!-- Pressed：背景更深 -->
                                                            <Trigger Property="IsPressed" Value="True">
                                                                <Setter TargetName="bg" Property="Fill" Value="#CCCCCC"/>
                                                            </Trigger>

                                                            <!-- Disabled 状态下淡化（可选） -->
                                                            <Trigger Property="IsEnabled" Value="False">
                                                                <Setter TargetName="icon" Property="Opacity" Value="0.4"/>
                                                                <Setter TargetName="bg" Property="Opacity" Value="0.4"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Button.Template>
                                            </Button>
                                        </Grid>

                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="true">
                                                <Setter Property="Panel.ZIndex" Value="1"/>
                                                <Setter Property="Visibility" TargetName="innerBorder" Value="Visible"/>
                                                <Setter Property="TextElement.Foreground" Value="{DynamicResource PrimaryBrush}" TargetName="contentPresenter"/>
                                                <Setter Property="Background" TargetName="BorderMask" Value="{DynamicResource RegionBrush}"/>
                                            </Trigger>

                                            <Trigger Property="hc:IconElement.Geometry" Value="{x:Null}">
                                                <Setter Property="Visibility" Value="Collapsed" TargetName="PathMain"/>
                                            </Trigger>

                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter Property="Opacity" Value="0.4" TargetName="contentPresenter"/>
                                            </Trigger>

                                            <!-- Hover 效果 -->
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <!--<Setter Property="Background" TargetName="mainBorder" Value="{DynamicResource SecondaryRegionBrush}" />-->
                                                <Setter Property="TextElement.Foreground" Value="{DynamicResource PrimaryBrush}" TargetName="contentPresenter"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>

                    </hc:TabControl.Resources>

                    <hc:TabControl.ItemContainerStyle>
                        <Style TargetType="hc:TabItem" BasedOn="{StaticResource CusTabItemPlusBaseStyle}">
                            <Setter Property="Menu">
                                <Setter.Value>
                                    <ContextMenu MinWidth="140" Padding="3,5" Background="{DynamicResource ThirdlyRegionBrush}">
                                        <ContextMenu.ItemContainerStyle>
                                            <Style TargetType="MenuItem">
                                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                                <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="MenuItem">
                                                            <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="2" Padding="0,5">
                                                                <ContentPresenter ContentSource="Header" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsHighlighted" Value="True">
                                                                    <Setter TargetName="Border" Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                                                    <Setter Property="Foreground" Value="{DynamicResource TextIconBrush}"/>
                                                                </Trigger>
                                                                <Trigger Property="IsEnabled" Value="False">
                                                                    <Setter Property="Opacity" Value="0.4"/>
                                                                    <Setter Property="Cursor" Value="No"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </ContextMenu.ItemContainerStyle>
                                        <MenuItem 
                                      Header="刷新页面"
                                      Command="{Binding PlacementTarget.DataContext.RefreshTabCommand, 
                                      RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                      CommandParameter="{Binding PlacementTarget.DataContext, 
                                      RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                        <MenuItem 
                                      Header="关闭当前"
                                      Command="{Binding PlacementTarget.DataContext.CloseTabCommand, 
                                      RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                      CommandParameter="{Binding PlacementTarget.DataContext, 
                                      RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                      IsEnabled="{Binding PlacementTarget.DataContext.IsClosable, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                        <MenuItem 
                                      Header="关闭左侧"
                                      Command="{Binding PlacementTarget.DataContext.CloseLeftTabsCommand, 
                                      RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                      CommandParameter="{Binding PlacementTarget.DataContext, 
                                      RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                      IsEnabled="{Binding PlacementTarget.DataContext.HasClosableLeft, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                        <MenuItem 
                                      Header="关闭右侧"
                                      Command="{Binding PlacementTarget.DataContext.CloseRightTabsCommand, 
                                      RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                      CommandParameter="{Binding PlacementTarget.DataContext, 
                                      RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                      IsEnabled="{Binding PlacementTarget.DataContext.HasClosableRight, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                        <MenuItem 
                                      Header="关闭其他"
                                      Command="{Binding PlacementTarget.DataContext.CloseOtherTabsCommand, 
                                      RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                      CommandParameter="{Binding PlacementTarget.DataContext, 
                                      RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                      IsEnabled="{Binding PlacementTarget.DataContext.HasClosableOther, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                        <MenuItem 
                                      Header="关闭全部"
                                      Command="{Binding PlacementTarget.DataContext.CloseAllTabsCommand, 
                                      RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                      CommandParameter="{Binding PlacementTarget.DataContext, 
                                      RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                      IsEnabled="{Binding PlacementTarget.DataContext.HasClosableAny, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                    </ContextMenu>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </hc:TabControl.ItemContainerStyle>

                    <hc:TabControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                                <!-- ContentControl：动态选择图标模板 -->
                                <ContentControl>
                                    <ContentControl.Style>
                                        <Style TargetType="ContentControl">
                                            <!-- 默认模板（AntDesign） -->
                                            <Setter Property="ContentTemplate" Value="{StaticResource AntDesignIconTemplate}" />
                                            <!-- 绑定数据上下文 -->
                                            <Setter Property="Content" Value="{Binding}" />
                                        </Style>
                                    </ContentControl.Style>
                                </ContentControl>

                                <!-- 文本标题 -->
                                <TextBlock Text="{Binding Header}" VerticalAlignment="Center"/>

                            </StackPanel>
                        </DataTemplate>
                    </hc:TabControl.ItemTemplate>

                    <!-- 内容模板绑定到 TabItemModel.Content -->
                    <hc:TabControl.ContentTemplate>
                        <DataTemplate>
                            <ContentPresenter Content="{Binding Content}"/>
                        </DataTemplate>
                    </hc:TabControl.ContentTemplate>
                </hc:TabControl>

            </Grid>
        </Border>

        <Border Grid.ColumnSpan="3"
                Background="#66000000"
                Panel.ZIndex="999"
                Visibility="{Binding IsAppLoading, Converter={StaticResource Boolean2VisibilityConverter}}" d:Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <Border Background="{DynamicResource RegionBrush}" CornerRadius="12" Padding="28,24">
                    <StackPanel HorizontalAlignment="Center">
                        <hc:LoadingCircle Width="56" Height="56" Foreground="{DynamicResource PrimaryBrush}"/>
                        <TextBlock Text="{Binding AppLoadingMessage}" Margin="0,16,0,0" FontSize="14" HorizontalAlignment="Center" Foreground="{DynamicResource PrimaryTextBrush}"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>
    </Grid>
</hc:Window>