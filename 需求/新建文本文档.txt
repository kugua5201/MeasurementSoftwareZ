测量软件首先要实现的功能：
1.测量数据源的创建，数据读取及交互，首先支持西门子S7、三菱MC、Modbust-cp、Modbus-rtu；
2.建立测量配方模型，测量通道绑定数据源；
3.测量功能和流程，包括测量图展示、通道信息展示、采集分析、结果、输出、状态、日志；
4.测量记录、查询；
5.测量SPC分析；
6.软件整体功能界面设计。
通道类型：结果值、最大值、最小值、平均值、跳动值、齿跳动值
->结果值：测量完成之后读取的最终值
->最大值、最小值、平均值、跳动值、齿跳动值：连续读取的数据记录后计算分析得出
界面上通道要展示的信息：通道编号、通道名称、通道说明、标准值、公差、测量值、结果




1、置零页面，手动置零，，自动置零=》绑定plc点位，上升沿，下降沿，值变化，，批量选中点位设定置零
2、测量页面趋势图，采集的点位的通道的趋势图，要吧校准值，plc采集值，都要缓存回来，趋势图要显示三个值，选中通道右键以后，测量值，如果启用了校准，校准值，还有置零值都要显示出来，三个缓存都要有
3、扫码触发的规则，开始-结束时间，有个延时时间，如果测量完成超过这个时间就，强制设置为ng，不管合格不合格，因为没扫码？并且启用了，不需要强制等待扫码结果
4、校准分为手动触发跟plc自动触发，在通道设置里面绑定
5、启动测量延时,工步延时，如果不是工步测量就是直接在其他设置里面加一个延时，加一个如果不是工步就是一个延时，如果是工步模式就在这个里面分别写入对应的延时，
6、高速滤波
